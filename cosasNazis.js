const Discord = require('discord.js')
const db = require('megadb')
let game = new db.crearDB('games')

var { error, deny } = require('./logs.js')

module.exports = {
    cosasNazis: async (client, message) => {

    }
}